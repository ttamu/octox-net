# httpd

## Usage

**Format:**

```sh
httpd [port] <root>
```

**Examples:**

```sh
$ httpd /etc
[httpd] octox-httpd/0.1
[httpd] document root: /etc
[httpd] listening on port 8080
[httpd] server started successfully
```
